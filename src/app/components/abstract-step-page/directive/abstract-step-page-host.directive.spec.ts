import { AbstractStepPageHostDirective } from './abstract-step-page-host.directive';

describe('AbstractStepPageHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractStepPageHostDirective();
    expect(directive).toBeTruthy();
  });
});
